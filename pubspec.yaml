name: register
description: "Mayak — Flutter app: social feed, jobs, Supabase backend."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.10.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.12.4
  url_launcher: ^6.3.0
  app_links: ^6.1.1
  flutter_lucide: ^1.11.0
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  flutter_timezone: ^5.0.2
  fl_chart: ^0.68.0
  image_picker: ^1.1.2
  image: ^4.2.0
  http: ^1.2.1
  http_parser: ^4.0.0
  permission_handler: ^12.0.3
  audioplayers: ^6.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
